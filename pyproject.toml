[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "sales-list-builder"
version = "0.1.0"
description = "Sales list builder tool"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]